apiVersion: v1
kind: Service
metadata:
  name: {{ include "qtcsystems.fullname" . }}
  labels:
    {{- include "qtcsystems.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qtcsystems.selectorLabels" . | nindent 4 }}
