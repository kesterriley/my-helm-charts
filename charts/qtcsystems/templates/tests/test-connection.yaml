apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qtcsystems.fullname" . }}-test-connection"
  labels:
    {{- include "qtcsystems.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qtcsystems.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
