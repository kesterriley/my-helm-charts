apiVersion: v1
kind: Service
metadata:
  name: {{ include "kdr-masterreplica.fullname" . }}
  labels:
    {{- include "kdr-masterreplica.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kdr-masterreplica.selectorLabels" . | nindent 4 }}
